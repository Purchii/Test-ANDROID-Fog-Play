# Decisions log

| ID | Date | Decision | Status | Rationale | Consequences |
|---|---|---|---|---|---|
| DEC-001 | 2026-06-05 | Use repository docs, not old chats, as source of truth. | accepted | Prevent context drift and stale decisions. | Codex must update docs for persistent rules. |
| DEC-002 | 2026-06-05 | Every independent bounded task starts in a fresh Codex thread. | accepted | Reduces context rot and keeps tasks isolated. | Completed threads become inactive. |
| DEC-003 | 2026-06-05 | Use strict multi-agent workflow for every bounded task. | accepted | User requires maximum quality and independent review. | If real subagents unavailable, task is blocked or needs explicit fallback approval. |
| DEC-004 | 2026-06-05 | One task branch per task, created from current default branch. | accepted | Prevents cross-task contamination and simplifies rollback. | Next task starts only after previous integration policy is satisfied. |
| DEC-005 | 2026-06-05 | In NON_AUTONOMOUS mode, default branch merge/push requires explicit user command. | accepted | Supervised safety. | Codex can push task branch but not trunk. |
| DEC-006 | 2026-06-05 | In BOUNDED_AUTONOMOUS mode, fully verified task branch may be merged/pushed to default branch. | accepted | Enables unattended progress with gates. | Requires tests, docs, QA/security approval, no R0/R1 blockers. |
| DEC-007 | 2026-06-05 | Thread title uses task ID + human title; branch uses same ID + Git-safe slug. | accepted | Better UI readability and Git traceability. | Temporary next-task thread must be renamed after Planner selects task. |
| DEC-008 | 2026-06-05 | GitHub remote was empty, so initial default branch is `main`. | accepted | No remote HEAD or branches existed; `main` is the GitHub default convention. | TASK-000 branches from `main`. |
| DEC-009 | 2026-06-05 | Task branches use the `qa/task-xxx-slug` format. | accepted | Starter pack and user preference allow `qa/`; it keeps QA task branches grouped. | Branch names must include task ID. |
| DEC-010 | 2026-06-05 | In `BOUNDED_AUTONOMOUS`, Codex may push verified default branch changes autonomously. | accepted | User authorized autonomous continuation beyond TASK-000. | Requires documented gates, no force-push and multi-agent review. |
| DEC-011 | 2026-06-05 | New independent tasks must start in fresh threads; current agents may be reused only when it does not reduce speed or quality. | accepted | Keeps task context clean while allowing efficient same-task review. | NEXT task after TASK-000 needs a fresh thread. |
| DEC-012 | 2026-06-05 | Public repo source-of-truth excludes raw reverse-analysis artifacts, archives and compiled cache files. | accepted | Repository is public and first commit defines public history. | Commit public-safe summaries only; exclude `raw/`, `.pyc` and zip by default. |
| DEC-013 | 2026-06-05 | Codex project thread title must match the currently executed task title. | accepted | Human-readable task names make project navigation and handoff safer than branch names or generic labels. | Every new task thread must be named like `TASK-001 - Runtime discovery and smoke bootstrap`. |
| DEC-014 | 2026-06-05 | Every task completion includes an explicit subagent closure audit. | accepted | Prevents inactive-thread agents from lingering after their review/handoff/debug purpose is finished. | Orchestrator must keep needed agents until outputs are captured, then close no-longer-needed agents before final task closure. |
| DEC-015 | 2026-06-06 | In autonomous mode, every completed verified task must be pushed to the remote default/trunk branch before the next independent task starts. | accepted | User explicitly authorized autonomous default-branch push and required completed tasks to reach master/default before the next task. | Codex must treat `master` wording as the detected default branch, currently `main`, and block next-task start if default push fails. |
| DEC-016 | 2026-06-30 | TASK-015D/016C uses a hard two-phase gate before inventory-only ADB. | accepted | Prevents ADB inventory collection or generated heuristic device inventory from being mistaken for approved TASK-005 runtime readiness. | Phase B remains blocked until Phase A passes; generated inventory stays heuristic/manual-review-required until separate owner/QA manual review. |
| DEC-017 | 2026-06-30 | TASK-015E/017 formalizes exact local path-family validation and full-tree hygiene scanning before TASK-005 can be considered. | accepted | Broad `.qa_local/` checks and diff-only whitespace checks can miss residual approval or repository hygiene gaps. | APK, synthetic secret and evidence paths must stay in their exact families; tracked text files must pass trailing whitespace, blank EOF and final-newline checks. |
| DEC-018 | 2026-06-30 | Public-safe generated inventory may be committed only as owner-review material, not as TASK-005 approval. | accepted | Sanitized inventory can support manual review but still comes from heuristic/generated classification. | Review exports must keep generated devices heuristic/manual-review-required and explicitly not approved until owner/QA manual review. |
| DEC-019 | 2026-06-30 | Approved TASK-005 metadata and owner-review exports use final strict schema and alias/API sanity gates. | accepted | Residual false-pass cases could otherwise make malformed local paths, aliases, duplicate lists or generated targets look approval-ready. | Unknown approved-metadata fields, broad path variants, stable Android-version aliases, Android/API mismatches, invalid forbidden scope and malformed owner-review inventory now block. |
| DEC-020 | 2026-06-30 | TASK-015G/017B adds residual pre-runtime approval strictness and owner input templates before TASK-005. | accepted | Independent post-TASK-015F/017A audit found lower-risk false-pass gaps that could still make malformed approval material appear runtime-ready. | TASK-005 approval metadata now requires a 30-day TTL, exact local path patterns, exact APK forbidden-action and target identifier policies, stricter no-auth synthetic policy validation, exact owner-review redaction/enum validation and public-safe owner approval inputs. |
| DEC-021 | 2026-07-01 | TASK-015H/017C is the final planned broad pre-runtime hardening polish before owner approval input. | accepted | The remaining audit findings are concrete false-pass cases; continuing broad hardening after they close risks delaying TASK-005 readiness without new evidence. | Future work should move to owner/QA target selection, local APK/hash/evidence approvals and a separate TASK-005 limited runtime task unless a new concrete false-pass is found. |
| DEC-022 | 2026-07-02 | Tests added or changed by a task must be debugged in the same task. | accepted | Prevents broken new tests from being deferred to a later independent task and keeps verification evidence local to the change that introduced the tests. | The relevant targeted test set must pass before task completion, or the task must record an explicit blocked verification note with the failing command, failure reason and why it cannot be fixed within scope. |
| DEC-023 | 2026-07-04 | Audit-chain owner instruction is persistent source-of-truth: each independent audit task uses a fresh thread, autonomous verified tasks are pushed to detected default branch, and the completed thread creates exactly one fresh continuation thread. | accepted | Owner explicitly authorized autonomous audit continuation, thread creation and push to `master`, interpreted by project policy as the detected default branch, currently `main`. | Subsequent audit threads must inherit this from repository docs, not old chat memory. TASK-025A still keeps no-device prohibitions: no ADB/runtime/APK/app/debug/raw evidence capture. |
| DEC-024 | 2026-07-06 | API-layer exhaustive coverage is split into offline contract exhaustiveness and separate approved live execution. | accepted | The owner-provided API audit pack contains useful schemas, fixtures and matrix rows, but also raw-value signals that must remain local-only. | TASK-028 may validate every known matrix row/fixture/schema in local quarantine and commit only aliases/counts/categories. Live API/backend/payment/session behavior remains `unknown`/`not_run` until separate `PROD_CONDITIONAL` approvals. |
| DEC-025 | 2026-07-06 | Static text coverage must distinguish source-reported likely counts from raw values actually available for local inventory. | accepted | The local sanitized static artifact reports a larger likely UI string count than the raw sample values it exposes. Treating the count as fully raw-covered would create a coverage false-pass. | TASK-035 may inventory every available raw sample value into ignored local storage and publish only aliases, counts, hash prefixes and categories. Missing raw full-list values must be recorded as a blocker, not inferred or reconstructed from APK/source/decompiled material. |
| DEC-026 | 2026-07-10 | Authoritative QA report evidence requires `evidence-report-envelope-v2` and inclusion in `report-manifest-v1`. | accepted | Audit findings F-004/F-005 showed that legacy JSON summaries lacked a shared authoritative evidence schema and manifest. Silent omission or loose schema inference can create release false confidence. | TASK-038 manifest records existing reports as explicit legacy migration blockers until converted to v2. Only v2-valid records can become authoritative, and duplicate authority, stale refs, hash drift, unknown schemas and unsafe values fail closed. |
| DEC-027 | 2026-07-10 | Release readiness must be derived from validated manifest authority, not self-asserted metadata. | accepted | Audit finding F-001/QA-P0-02 showed the older release gate path could report PASS from asserted gate/reviewer strings without artifact provenance. | TASK-039 release readiness remains blocked unless required R0/R1 gates are backed by authoritative v2 records with confirmed evidence, reviewer approval, valid hashes, evidence storage and cleanup/rollback prerequisites. |
| DEC-028 | 2026-07-10 | Launch every new top-level task or continuation dialog on model identifier `gpt-5.6-sol` (display name `5.6 Sol`) with reasoning effort `high`. | accepted | Owner set the model profile for future Codex dialogs. | Fresh-thread creation and handoff must request `gpt-5.6-sol` / `high`; current-task subagent roles continue to use the available multi-agent tooling. |
| DEC-029 | 2026-07-17 | Official repository export authority is a machine-readable, hash-bound index embedded in the export and produced/validated by tracked QA automation without requiring `.git` in the exported tree. | accepted | Git-only discovery cannot prove completeness or integrity after an official ZIP is unpacked, while committing a checkout-specific generated export index would create avoidable drift. | Export validation must fail closed on missing/stale/malformed indexes, unindexed or missing files, duplicate/unsafe paths, traversal, forbidden content and unsafe symlinks; the same relevant checks run in a normal checkout and no-`.git` export. |
| DEC-030 | 2026-07-17 | Integrate the TASK-041…055 archive only through its verified `PUBLIC_SAFE_QA_OVERLAY/` payload and preserve current repository authority on collisions. | accepted | The supplied archive explicitly keeps `RUN_PACKS/` external, while the root README collision and broad future-runtime wording can erase or weaken current controls if copied verbatim. | Use fresh ignored containment/hash-verified staging, merge the root README additively, keep TASK-041 repository-only and represent TASK-042…055 runtime actions as separately gated future work. |
| DEC-031 | 2026-07-17 | TASK-041…055 task/run discovery uses a separate tracked machine-readable epic index with explicit repository-relative spec and scenario-catalog links. | accepted | Human-readable plans alone can silently omit a task, catalog, dependency or continuation edge. | Epic validation requires exactly 15 tasks, 15 catalogs, 307 unique scenarios, the canonical dependency DAG and explicit next-task links; this planning authority does not prove runtime execution or release readiness. |
| DEC-032 | 2026-07-29 | TASK-043 regression selection is a fixed-path `PROD_SAFE_OFFLINE_STATIC_ONLY` authority that keeps historical evidence stale by default and separates static selector success from product/runtime coverage. | accepted | Mapped, blocked, legacy, AVD, synthetic, recovery and cross-family evidence can otherwise create false release confidence. | The selector validates 55 opaque surfaces against 307 scenarios, distinguishes five APK families from the launcher contour, emits only selection/gap authority and requires later task-local runtime evidence; local verification may finish before remote integration when the owner temporarily defers pushes. |
| DEC-033 | 2026-08-14 | Treat `.qa_local` as checkout-local and require a bounded same-repository worktree recovery gate before declaring approved APK/device/fixture material missing. | accepted | TASK-044 initially saw an empty fresh-worktree local contract even though the owner checkout contained the five-APK bundle, ADB mapping and synthetic fixture. | Fresh tasks check only known Git worktrees, restore only approved ignored subtrees, validate exact canonical contracts and freshness, keep raw values local, and request an explicit alternate root only when bounded recovery cannot satisfy the contract. |
| DEC-034 | 2026-08-15 | TASK-045 may use the sole owner-selected mapped `phone-realme-001` with its owner-confirmed existing newer `phone-full` install only for independent disconnected phone inventory, never as canonical-build or paired-lane evidence. | accepted | The required TV is unavailable and the one ordinary canonical phone-full install/update was rejected as a version downgrade; deleting data, uninstalling or overriding downgrade safety would exceed the approved contour. | Existing-newer runtime keeps canonical compatibility `unknown_not_verified`; QA-045-006/009 need their own oracles, every paired/connected row remains blocked by the missing TV, and no historical manual-review status or primary/fallback lane equivalence is changed. |
| DEC-035 | 2026-08-15 | TASK-045A treats Phone Full as an independent visual screen/state/transition graph and never promotes TASK-045 terminal-ledger or Television Full evidence into fresh Phone Full coverage. | accepted | Phone and TV UIs/transitions differ, and the owner requires direct visual verification for every approved reachable Phone Full node and edge. | Historical 20 PNG/19 XML/19 logs remain audit-only, cp001 remains incomplete, and fresh `covered` rows require run-window visual screenshot review plus UI tree and bounded target-app log/marker; unproven session-dependent rows stay `blocked_by_external_state`. |
| DEC-036 | 2026-08-15 | After TASK-045A verified remote-default closure, the completed thread creates exactly one fresh continuation thread and then sends it no follow-up/ping/wake message and performs no polling. | accepted | Owner requires autonomous continuation without duplicate task activation or old-thread steering after successful creation. | TASK-046 cannot start before TASK-045A closure; the initial creation prompt must carry the complete source-of-truth handoff, and the old thread becomes inactive immediately after successful creation. |
| DEC-037 | 2026-08-15 | TASK-048 closes only a repository authority while physical FogPlay Stick and launcher/system runtime remain blocked. | accepted | TASK-042/TASK-043 dependencies are complete, but physical Stick availability, compatible AOSP artifact, launcher mapping and fixture are unknown; generic TV/phone/AVD substitution would create false coverage. | The verified authority keeps 19/19 terminal rows with 17 blocked_by_device, QA-048-014 blocked_by_product_boundary and QA-048-019 static-only observed_pass; runtime/product coverage stays zero, `BLOCK_RUNTIME` and `blocks_release` remain authoritative, and the next thread performs fresh task selection because TASK-046/TASK-047 and dependent TASK-049 remain blocked. |
| DEC-038 | 2026-08-15 | Reprioritize the near-term QA program exclusively to complete Phone Full end-to-end coverage on the owner's sole available physical phone. | accepted | YandexTV, SberBox, AOSP Stick, generic TV and other device/APK resources are currently unavailable, while TASK-045/TASK-045A expose substantial unresolved phone coverage. | TASK-057…063 form the sequential phone-only roadmap. Existing TASK-041…055 history and statuses remain unchanged; unavailable-family work is `deferred_by_owner_resource_policy_2026-08-15`, not completed. Phone evidence never satisfies TV/Stick/cross-family/five-APK claims, and no phone runtime begins before fresh device/build/synthetic-fixture/first-launch/evidence-cleanup/Security gates pass. |
| DEC-039 | 2026-08-15 | The phone-only roadmap uses a lossless append-only crosswalk for every TASK-045 phone row and TASK-045A branch row. | accepted | Merging historical rows or treating terminal closure as fresh coverage could hide first-launch, visual, boundary, paired or cleanup gaps. | TASK-063 requires all 26 plus 17 rows exactly once, preserves A001 as audit-only and paired/TV rows as deferred, rejects missing/duplicate/merged ownership, and requires approved reachable Phone Full rows to be freshly covered or release-blocking blocked. |
| DEC-040 | 2026-08-16 | Permit exactly one target-scoped Phone Full uninstall and one ordinary install of selected `main-apk-03` in TASK-057R, accepting only that target application's local data/session loss. | accepted | The owner explicitly authorized this bounded recovery after TASK-057 confirmed installed/candidate signing incompatibility and accepted the unavoidable target-app local-state loss. | Pre-action Security plan GO and a one-shot stop/no-retry contingency are mandatory; recovery after uninstall/install failure needs new owner authority. This is not authority for separate clear-data, device reset, another package, downgrade bypass, APK modification, launch/navigation or TASK-058. Lost target data is not restored and no rollback is claimed. Successful reinstall may pass only rows 01–04; rows 05–07 still require their independent passports and Security `GO_RUNTIME`. |
| DEC-041 | 2026-08-16 | For TASK-058 use only owner-selected public alias `task058-selected-phone-full-001`, superseding `main-apk-03` for this task's package action, with one exact-target uninstall, one ordinary install and zero retry. | accepted | The owner supplied one exact ignored/local-only same-repository candidate and accepted loss of only the freshly mapped target application's local data/session. | Security must approve the exact package plan before action. Drift, ambiguity or failure stops without retry or alternate artifact. Install success cannot infer any of the seven readiness rows; launch still requires all seven fresh passes plus Security `GO_RUNTIME`. |
| DEC-042 | 2026-08-16 | Close TASK-058 without launch after the post-install raw-path spill and the independently missing runtime passports. | accepted | The one-shot package action succeeded through installed package presence, but exact equivalence/delta/final-selector checks were interrupted and Security already held `BLOCK_RUNTIME` for rows 05–07. | No retry, alternate artifact or launch is allowed. Readiness is 2 pass/5 blocking, all three inherited rows remain terminal-blocked, TASK-059 stays blocked, and tracked output publishes only aliases/categories/statuses/evidence ids. |
| DEC-043 | 2026-08-16 | Permit one hash-bound TASK-058A runtime under an explicit owner waiver of selector and unrelated-package-delta revalidation after the one-shot collector failed closed. | accepted | The owner confirmed that the installed app is the supplied same build, explicitly authorized testing it, verbatim waived selector/delta revalidation and accepted drift risk; Security reviewed that exact statement and issued `GO_RUNTIME_OWNER_OVERRIDE`. | This is not the legacy seven-of-seven GO. Readiness row 03 remains `evidence_status=unknown` with owner-override reason metadata, the waiver is not reusable, no collector retry/reinstall/clear-data/reset is allowed, overall release remains blocked and TASK-059 is not authorized. |
| DEC-044 | 2026-08-16 | Treat the owner-approved installed-never-launched state as a one-use clean-first-launch fixture and stop at the first authentication boundary. | accepted | One launch irreversibly consumes the fixture, while credential entry/authentication and TASK-059 work are outside TASK-058A authority. | TASK-058A may record the observed pre-auth login/auth guard and launch transition, then must force-stop the target, return Home and stop capture. The never-launched state cannot be restored under current authority and no rollback claim is permitted. |
| DEC-045 | 2026-08-16 | Execute EPIC-PHONE-001 as one fresh bounded epic in one thread and branch, with TASK-059 through TASK-062 superseded as internal stages rather than separate tasks. | accepted | Owner direction requires one terminal coverage ledger for the full authorized phone application surface and forbids splitting the four objectives into independent continuation threads. | The epic branch is `qa/epic-phone-001-full-mobile-application-test-coverage`; TASK-058/TASK-058A history remains immutable; a repository-only blocked ledger may close when safety authority is absent, but it cannot claim product coverage or release PASS. Runtime/auth stays blocked until the exact fixture is explicitly classified fully synthetic/test-only and Security issues a fresh literal contour/run-bound GO; neither availability, TASK-058A's one-run override nor plan-only budgets are reusable authority. |
| DEC-046 | 2026-08-16 | Accept category-only owner classification of `epic-phone-001-fixture-001` as the synthetic fixture authority for the current epic run only. | accepted | The owner explicitly confirmed that the fixture is fully test-only, unrelated to real users, current build/environment/phone-bound and free of billing/payment/subscription/entitlement impact. | Synthetic-session creation/termination, read-only navigation and safe logout may become eligible only after contour-specific Security GO. Values remain local-only/redacted. Payment, subscriptions, entitlement/profile/account mutation, paid sessions and external/QR traversal remain forbidden. This decision is not a GO and expires with completion/revocation. |
| DEC-047 | 2026-08-16 | Resume EPIC-PHONE-001 in the same thread/branch through separate one-shot C0P local-presence and C1 launch-free readiness gates before any runtime/auth contour. | accepted | Category-only fixture authority resolves classification but cannot prove local field presence, controller identity or target/build/passport bindings. | Repository controller construction is allowed; C0P/C1/runtime/auth stay blocked until their exact Security plans and literal hash-bound expiring tokens are independently issued. Controller/public output must fail closed, remain category-only and never self-issue GO. |
| DEC-048 | 2026-08-18 | Accept the final owner-local fixture provisioner snapshot as repository logic only and fail closed after its prepared authorities expired. | accepted | Executor/loader/test hashes `f47d9776…` / `1cf7ebc…` / `b9c92bf…` passed 40 focused tests, the earlier combined EPIC set passed 168, and QA-A/QA-B/Security repository R0/R1 are `0/0`; however all prepared authorities expired at `2026-08-18T05:50:28Z` before any GO. | Expired artifacts are immutable and non-replayable; fixture write, C0P, C1, auth and runtime remain blocked with zero secret/device/runtime counters. Renewal uses create-new versioned artifacts only. |
| DEC-049 | 2026-08-18 | Use Security-resolved renewal identities `authority-renewal-001`, `c0p-authority-003`, `c0p-prep-003` and `security-c0p-003` under fixed `authority-sets/c0p-authority-003` paths. | accepted | Reconciles the earlier Security generation-`002` and Planner authority-`003` naming conflict without allowing an agent to choose or infer authority. | This decision is naming/path authority only; the identities are not GO. Final provisioner source/HEAD rebind plus renewal candidate required one joint review and one implementation commit, satisfied by `2ca38ae9fff08550a0be533f9d8d934b8c7b7da6`; no interim provisioner commit or in-place renewal occurred. |
| DEC-050 | 2026-08-18 | Accept the final joint authority-renewal/C0P/controller/provisioner rebind snapshot for one repository commit and epic-branch push only. | accepted_repository_only_implemented | Anomalies 071–082 are closed at repository level; exact source hashes are bound in current-state/active-run; core 144 plus named safety 14 equals combined 158, with compile/diff PASS. QA-A is `0/0/1`, QA-B `0/0/0`, Security `0/0/1`; the sole P2 is cooperative timeout. Implementation commit `2ca38ae9fff08550a0be533f9d8d934b8c7b7da6` is pushed/aligned on the epic branch. | `c0p-prep-003 --validate-only` is superseded by renewal. No GO or execution exists, expired authorities remain immutable/non-replayable and checkpoint-local conditional counters remain zero. The post-doc final HEAD must be used to rebuild the canonical candidate/plan and obtain fresh owner/Security authority; do not integrate default. |
| DEC-051 | 2026-08-18 | Close consumed renewal001/set003 and accept generation004 as repository-only implementation. | accepted_repository_only_implemented | Renewal001 on HEAD `92a60f8d585d5887a465563902c66a2aa2b373b4` materialized 4 artifacts/2 directories/6 files with forbidden 0; its GO is consumed and passports expired at `2026-08-18T10:44:00Z`. Generation004 passes all recorded checks/reviews and implementation commit `6637e074555f1ff501c3beae8cdb5b8fb3d9a361` is pushed/aligned on the epic branch. | Worktree was clean after push. Remove/unstage expired generic renewal001 inputs and retain hashes only. Generation004 has no GO and set004 is not materialized. The post-doc final authority-binding HEAD is required for renewal002 with owner no-mutator002, combined weekly provision envelope, canonical plan and fresh Security GO. |
| DEC-052 | 2026-08-20 | Treat renewal002/set004 as successfully materialized but close the single owner-local provision launch fail-closed because its parent terminal aggregate was not retained. | accepted_fail_closed_nonreusable | Renewal002 on HEAD `efc6e85060e15d2d5fd0d4396e0960fbdd56bea8` returned four artifacts/one directory/six files and forbidden 0. The metadata preflight returned parent/destination `absent`; after one visible-console provision launch lost its parent projection to truncation, a separate authorized check returned marker/destination `absent_at_checkpoint`. | The exact launch budget is exhausted. Provision GO and set004 passports expired `2026-08-20T11:44:25Z` and cannot be retried, extended or reused. Confirmed mutation evidence count is zero; historical/transient mutation is `unknown_not_evidenced`, and checkpoint absence is never proof of no mutation. Console value entry/consumption stays `unknown`. C0P/device/app/auth/runtime/network/payment remain not run/zero, so release remains blocked. |
| DEC-053 | 2026-08-20 | Accept generation005 as repository code only with separate console-readiness and provision one-shots, while withholding every execution/runtime authority. | accepted_repository_code_only_implemented_no_go | Exact ten-file hashes/sizes are recorded in current-state/active-run. Focused 200 and safe 1639/4 pass; unfiltered 1658/4/17 is environment-blocked only by unavailable TASK-045 local evidence. Public safety 443/0, both hygiene modes, docs 187/0, compile and diff pass. QA-A is `0/0/0 GO_REPOSITORY_ONLY`, QA-B `0/0/0 GO` candidate, Security `0/0/1 GO_REPOSITORY_CODE_ONLY`. Implementation commit `d99073a7465f370ff6e39713005f6d6d09464849` is pushed/aligned on the epic branch and the worktree was clean after push. | Use only `authority-renewal-003`, set005/prep005/security-c0p005, provision003 and independent readiness001 fixed paths. Expected-GO builders do not issue GO. The post-doc lifecycle commit becomes final authority-binding HEAD; fresh owner003 and distinct Security GO are required per contour. Marker-only/result-best-effort attempts are consumed with no retry. No runtime GO or default integration; `origin/main` remains `b268b1f198f595ec835e066169c97cdf839cc05b`. |
