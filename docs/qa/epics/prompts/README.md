# Codex prompts — TASK-041…055

Each file is the canonical fresh-thread execution prompt after the epic overlay
is integrated.

Sequence:

```text
task041_codex_prompt.md
...
task055_codex_prompt.md
```

The completed task thread creates exactly one next fresh thread and points it to
the next prompt. Do not execute multiple independent tasks in one thread.
